---
title: "Home"
format: html
page-layout: full
---

:::: hero
::: overlay
### Coexistence based on Knowledge and <br> Conservation of Indigenous Territories
:::
::::

```{=html}
<style>

/* ========== NAV BAR FONT ========== */

/* Override navbar just on the homepage */
.navbar,
.navbar .nav-link,
.navbar .nav-link:hover,
.navbar .nav-link:focus,
.navbar .nav-link.active,
.navbar-brand,
.navbar-brand:hover {
  color: #ffffff !important;
}

h3 {
  color: #ffffff !important;
  font-size: 2.25rem;
}

h3 .anchorjs-link {
  display: none !important;
}

/* ========== GLOBAL / LAYOUT ========== */

/* No scroll; no stray gaps */
html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  overflow: hidden;      /* lock page scroll */
  overflow-x: hidden;    /* prevent viewport overshoot white strip */
}

/* Quarto sometimes injects top padding when navbar is fixed */
body.nav-fixed { padding-top: 0 !important; }

/* Kill default gutters/margins so the hero truly sits at the edges */
#quarto-content,
main#quarto-document-content,
.page-columns, .content, .column-page {
  margin: 0 !important;
  padding: 0 !important;
  max-width: 100% !important;
}

/* ========== NAVBAR ON TOP, TRANSPARENT ========== */
.navbar {
  position: fixed !important;
  top: 0; left: 0; right: 0;
  background: transparent !important;
  box-shadow: none !important;
  z-index: 1000; /* above hero */
}

/* Optional: turn solid after scroll (kept harmless since scrolling is locked) */
/*
.headroom--not-top .navbar {
  background: #fff !important;
  box-shadow: 0 2px 6px rgba(0,0,0,.1);
}
*/

/* ========== HERO (FULL-SCREEN BACKGROUND) ========== */

/* Pin hero to the viewport so it always fills it and sits behind navbar */
.hero {
  position: fixed;  /* attach to viewport */
  inset: 0;         /* top:0 right:0 bottom:0 left:0 */
  background: url("images/home.jpg") center / cover no-repeat;
  z-index: 0;
}

/* Centered overlay content */
.hero .overlay {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;          /* vertical center */
  justify-content: flex-start;  /* align to left */
  padding-left: 5%;            /* push text in 10% from left edge */
  padding-top: 25%;
  text-align: left;
}

/* ========== MOBILE RESPONSIVE STYLES ========== */

/* Mobile styles for screens smaller than 768px */
@media (max-width: 992px) {
  .hero .overlay h3 {
    font-size: 1.75rem !important;
  }
  
  .hero .overlay h3 br {
    display: none !important;
  }
  
  .hero .overlay {
    padding-left: 15%;
    padding-right: 15%;
    padding-top: 75%;
    text-align: center;
  }
  
  /* Make mobile navbar expand like other pages but with blur */
  .navbar-collapse {
    background-color:rgba(99, 111, 135, 0.55) !important;
    backdrop-filter: blur(10px) !important;
    -webkit-backdrop-filter: blur(20px) !important;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15) !important;
    width: 100vw !important;
    height: 100vh !important;
    margin-left: calc(-50vw + 50%) !important;
    margin-right: calc(-50vw + 50%) !important;
    padding-left: 15px !important;
    padding-right: 15px !important;
    padding-top: 25% !important;
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    z-index: 1050 !important;
  }
  
  /* Hide the main navbar when mobile menu is open */
  .navbar-collapse.show ~ .navbar-brand,
  .navbar-collapse.show ~ .navbar-toggler {
    display: none !important;
  }
  
  /* Add custom X button inside the navbar-collapse */
  .navbar-collapse::before {
    content: "✕" !important;
    position: absolute !important;
    top: 40px !important;
    left: 40px !important;
    color: white !important;
    font-size: 24px !important;
    font-weight: bold !important;
    cursor: pointer !important;
    z-index: 1060 !important;
    line-height: 1 !important;
  }
  
  .navbar-collapse {
    text-align: center !important;
  }
  
  .navbar-collapse .nav-link {
    color: white !important;
    text-align: center !important;
    font-size: 1.2rem !important;
    margin: 10px 0 !important;
    display: block !important;
    width: auto !important;
  }
  
  .navbar-collapse .nav-link:hover,
  .navbar-collapse .nav-link:focus {
    color: white !important;
    text-decoration: underline !important;
  }
  
  .navbar-collapse .nav-link.active {
    color: white !important;
    text-decoration: underline !important;
  }
}

</style>
```

```{=html}
<script>
// Make the custom X button clickable
document.addEventListener('DOMContentLoaded', function() {
  // Add click event to the navbar-collapse when it's shown
  const navbarCollapse = document.querySelector('.navbar-collapse');
  const navbarToggler = document.querySelector('.navbar-toggler');
  
  if (navbarCollapse && navbarToggler) {
    navbarCollapse.addEventListener('click', function(e) {
      // Check if the click is on the X button (top-left area)
      const rect = navbarCollapse.getBoundingClientRect();
      const clickX = e.clientX - rect.left;
      const clickY = e.clientY - rect.top;
      
      // If click is in the top-left area (where X is positioned)
      if (clickX <= 60 && clickY <= 60) {
        // Trigger the navbar toggler to close the menu
        navbarToggler.click();
      }
    });
  }
});
</script>
```
